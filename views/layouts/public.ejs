<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | <%= appName %></title>
  <meta name="description" content="AI-powered sales training platform. Master sales techniques with real-time AI coaching.">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary-blue: #0B1F3B;
      --accent-orange: #FF6A00;
      --neutral-gray: #F4F6F8;
      --success-green: #10B981;
    }
    body { font-family: 'Inter', sans-serif; }
    .navbar { background: rgba(255,255,255,0.95); backdrop-filter: blur(10px); }
    .navbar-brand { font-weight: 700; color: var(--primary-blue); }
    .btn-primary { background: var(--accent-orange); border-color: var(--accent-orange); }
    .btn-primary:hover { background: #e55f00; border-color: #e55f00; }
    .btn-outline-primary { color: var(--primary-blue); border-color: var(--primary-blue); }
    .btn-outline-primary:hover { background: var(--primary-blue); color: #fff; }
    .hero-section {
      background: linear-gradient(135deg, var(--primary-blue) 0%, #1a365d 100%);
      color: #fff;
      min-height: 90vh;
      display: flex;
      align-items: center;
    }
    .hero-title { font-size: 3.5rem; font-weight: 800; line-height: 1.1; }
    .hero-subtitle { font-size: 1.25rem; opacity: 0.9; }
    .feature-card { border: none; border-radius: 16px; padding: 2rem; height: 100%; transition: transform 0.2s, box-shadow 0.2s; }
    .feature-card:hover { transform: translateY(-4px); box-shadow: 0 12px 24px rgba(0,0,0,0.1); }
    .feature-icon { width: 64px; height: 64px; border-radius: 16px; display: flex; align-items: center; justify-content: center; font-size: 1.75rem; margin-bottom: 1.5rem; }
    .pricing-card { border: 2px solid #e5e7eb; border-radius: 16px; padding: 2rem; transition: border-color 0.2s, transform 0.2s; }
    .pricing-card.featured { border-color: var(--accent-orange); transform: scale(1.05); }
    .pricing-card:hover { border-color: var(--accent-orange); }
    .industry-badge { padding: 0.75rem 1.5rem; border-radius: 50px; background: #f1f5f9; font-weight: 500; display: inline-flex; align-items: center; gap: 0.5rem; transition: background 0.2s; }
    .industry-badge:hover { background: #e2e8f0; }
    footer { background: var(--primary-blue); color: #fff; }
    footer a { color: rgba(255,255,255,0.7); text-decoration: none; }
    footer a:hover { color: #fff; }
  </style>
  <%- typeof head !== 'undefined' ? head : '' %>
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg fixed-top shadow-sm">
    <div class="container">
      <a class="navbar-brand" href="<%= basePath %>/">
        <i class="bi bi-lightning-charge-fill text-warning"></i>
        <%= appName %>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item"><a class="nav-link" href="<%= basePath %>/features">Features</a></li>
          <li class="nav-item"><a class="nav-link" href="<%= basePath %>/pricing">Pricing</a></li>
          <li class="nav-item"><a class="nav-link" href="<%= basePath %>/demo">Demo</a></li>
        </ul>
        <div class="d-flex gap-2">
          <a href="<%= basePath %>/auth/login" class="btn btn-outline-primary">Login</a>
          <a href="<%= basePath %>/auth/register" class="btn btn-primary">Start Free</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <main>
    <%- body %>
  </main>

  <!-- Footer -->
  <footer class="py-5 mt-5">
    <div class="container">
      <div class="row g-4">
        <div class="col-lg-4">
          <h5 class="fw-bold mb-3">
            <i class="bi bi-lightning-charge-fill text-warning"></i>
            <%= appName %>
          </h5>
          <p class="opacity-75">AI-powered sales training platform that helps teams master sales techniques through real-time coaching and practice.</p>
        </div>
        <div class="col-lg-2">
          <h6 class="fw-bold mb-3">Product</h6>
          <ul class="list-unstyled">
            <li class="mb-2"><a href="<%= basePath %>/features">Features</a></li>
            <li class="mb-2"><a href="<%= basePath %>/pricing">Pricing</a></li>
            <li class="mb-2"><a href="<%= basePath %>/demo">Demo</a></li>
          </ul>
        </div>
        <div class="col-lg-2">
          <h6 class="fw-bold mb-3">Resources</h6>
          <ul class="list-unstyled">
            <li class="mb-2"><a href="#">Blog</a></li>
            <li class="mb-2"><a href="#">Help Center</a></li>
            <li class="mb-2"><a href="#">API Docs</a></li>
          </ul>
        </div>
        <div class="col-lg-2">
          <h6 class="fw-bold mb-3">Company</h6>
          <ul class="list-unstyled">
            <li class="mb-2"><a href="<%= basePath %>/about">About</a></li>
            <li class="mb-2"><a href="<%= basePath %>/contact">Contact</a></li>
            <li class="mb-2"><a href="#">Careers</a></li>
          </ul>
        </div>
        <div class="col-lg-2">
          <h6 class="fw-bold mb-3">Legal</h6>
          <ul class="list-unstyled">
            <li class="mb-2"><a href="<%= basePath %>/privacy">Privacy</a></li>
            <li class="mb-2"><a href="<%= basePath %>/terms">Terms</a></li>
          </ul>
        </div>
      </div>
      <hr class="my-4 opacity-25">
      <div class="d-flex justify-content-between align-items-center">
        <p class="mb-0 opacity-75">&copy; <%= currentYear %> <%= appName %>. All rights reserved.</p>
        <div class="d-flex gap-3">
          <a href="#"><i class="bi bi-twitter"></i></a>
          <a href="#"><i class="bi bi-linkedin"></i></a>
          <a href="#"><i class="bi bi-youtube"></i></a>
        </div>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <%- typeof scripts !== 'undefined' ? scripts : '' %>
</body>
</html>
