<%- include('../../layouts/admin', { body: `

<div class="container-fluid py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1 class="h3 mb-0"><i class="bi bi-diagram-3 me-2"></i>Logic Rules</h1>
      <p class="text-muted mb-0">Configure conditional AI responses</p>
    </div>
    <button class="btn btn-primary">
      <i class="bi bi-plus-lg me-1"></i> Add Rule
    </button>
  </div>

  <div class="card">
    <div class="card-body text-center py-5 text-muted">
      <i class="bi bi-diagram-3 fs-1 d-block mb-2"></i>
      No logic rules configured yet.<br>
      <small>Add rules to customize AI behavior based on conditions.</small>
    </div>
  </div>
</div>

` }) %>
